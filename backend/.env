# Backend server port
PORT=8787

# Database connection
DB_URL=postgresql://rag:ragpwd@127.0.0.1:5432/analytics

# Multi-provider AI config (single-line JSON). Do NOT include endpoint paths like /chat/completions in baseUrl.
AI_PROVIDERS=[{"provider":"xxx","baseUrl":"xxx","apiKey":"sk-xxx","models":["xxx"],"defaultModel":"xxx"}]

# Legacy single-provider fallback (ignored when AI_PROVIDERS is set)
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=xxx

AI_DEBUG=true